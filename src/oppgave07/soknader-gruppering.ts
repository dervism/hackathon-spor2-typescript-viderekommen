import type {ForeldrepengerSoknad} from '../domain/foreldrepenger-soknad.js';

/**
 * Oppgave 7 — Grupper og sorter (15 poeng)
 *
 * Returner en Map fra postnummer til en sortert liste av sakene som
 * hører til. Listene skal være sortert alfabetisk på etternavn.
 */
export function grupperPaPostnummer(soknader: ReadonlyArray<ForeldrepengerSoknad>): Map<string, ForeldrepengerSoknad[]> {
  // TODO
  throw new Error('Oppgave 7 — ikke implementert ennå');
}
