# Hjelp — Oppgave 11: GET /saker

Hono kobler URL-pattern og handler-funksjon direkte. Stub-en er
allerede satt opp; du fyller inn handler-en og returnerer alle saker
som JSON.

Test fra terminalen mens appen kjører (`npm run dev`):

    curl http://localhost:8787/saker
